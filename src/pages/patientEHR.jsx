import react from "react";
import { Container, Button } from "react-bootstrap";

const ehrprofile = () => {
  return (
    <Container>
      <Button>Open Modal</Button>
    </Container>
  );
};
export default ehrprofile;
